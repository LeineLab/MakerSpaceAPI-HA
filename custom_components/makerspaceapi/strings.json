{
  "config": {
    "step": {
      "user": {
        "title": "Connect to MakerSpaceAPI",
        "description": "Enter the base URL for your MakerSpaceAPI instance. The API token is optional: without it only product stock and prices are available. With a device token, booking target balances and rental item status are also exposed.",
        "data": {
          "url": "Base URL",
          "token": "API Token"
        },
        "data_description": {
          "url": "Base URL without trailing slash",
          "token": "Bearer token from the machine registration page"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot connect to the API. Check the URL and your network, then try again."
    },
    "abort": {
      "already_configured": "This MakerSpaceAPI instance is already configured."
    }
  }
}
